# Google Form to export from
SOURCE_FORM_ID=your_source_form_id_here

# Existing Google Form to replace during JSON import (optional for export-only usage)
TARGET_FORM_ID=

# Google Drive folder ID for exported files
EXPORT_FOLDER_ID=your_folder_id_here

# Google Drive file ID of a JSON export to import (optional for export-only usage)
IMPORT_FILE_ID=
